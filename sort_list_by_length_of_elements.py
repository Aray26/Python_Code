print "What are the elements you want to cut up?"
x = raw_input().split(' ')

dictionary = {}

for i in x:
    dictionary[i] = len(i)
    print i
    print len(i)
    
print "dictionary = " + str(sorted(key=lambda dictionary(keys): dictionary(values))


#(key=lambda x: x[1])
    
    
      